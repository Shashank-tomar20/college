#include <stdio.h>
int main()
{
    int v;
    printf ("enter an integer:");
    scanf ("%d",&v);
    if (v<0)
    {
        printf ("you have entered %d.\n",v);
    }
    
    
    printf( "the if statement is easy");

    return 0;
}
